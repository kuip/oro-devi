/*var FCluster = new DC.AppCluster({})
var SSV = fileCluster.addApp('SimpleServerViewer', 'viewer')
  .addComponent('fileViewer', 'server', function(params, req, res, next) {
    var file = OroFile.findOne(params.id)

  })

ssv.addRoute('/api/file/:id', ssv.components.fileViewer)
ssv.components.fileViewer.

*/